# ── OS ──
.DS_Store
Thumbs.db
desktop.ini

# ── エディタ / IDE ──
.idea/
.vscode/
*.iml
*.swp
*.swo
.project
.classpath
.settings/

# ── ログ / 一時ファイル ──
*.log
logs/
tmp/
*.tmp

# ── 環境変数・秘密情報 ──
.env
.env.*
!.env.example

# ── 依存 / ビルド成果物（言語別。/project-setup が言語に応じて追記する）──
# ※ロックファイル（package-lock.json / uv.lock / Cargo.lock 等）は無視しない＝コミットする（.claude/rules/git-workflow.md「依存ロックファイル」）
node_modules/
dist/
build/
out/
coverage/
__pycache__/
*.py[cod]
.venv/
venv/

# ── Obsidian プラグインのビルド成果物（main.js は GitHub release に添付・リポジトリには含めない）──
main.js
*.js.map

# ── ビジュアル検証プレビューのバンドル（生成物。ソース scripts/preview/*.tsx/.html はコミットする）──
scripts/preview/preview.bundle.js

# ── Claude Code（ローカル設定はコミットしない）──
.claude/settings.local.json
# /remember・継続メモリのハンドオフ記録（チームで共有する場合はこの行を外す）
.remember/

# 一時的なスクリーンショット用ハーネス（#105 のビジュアル確認・コミット対象外）
.screenshot-harness/
